'use strict';const test=require('node:test'),assert=require('node:assert/strict'),w=require('../services/fieldworkWorkflow'),p=require('../services/passwords');const base={siteKey:'reef-1',colonyKey:'c-1',interventionType:'outplant',surveyId:'s-1',sourceRecordId:'off-1',sourceSha256:'a'.repeat(64),imageSha256:'b'.repeat(64),datasetVersion:'d1',modelVersion:'m1',deviceId:'dev1',speciesTaxon:'Acropora cervicornis',healthClass:'healthy',survivalStatus:'alive',initialSizeCm:10,currentSizeCm:12,latitude:24.5,longitude:-81.2,accuracyM:2,capturedAt:'2026-01-01',season:'dry'};
test('validates geospatial provenance and growth',()=>{const r=w.normalize(base,new Date('2026-01-02'));assert.equal(r.growthCm,2);assert.equal(r.speciesTaxon,'Acropora cervicornis');});test('missing measurements require reason',()=>assert.throws(()=>w.normalize({...base,currentSizeCm:null},new Date('2026-01-02')),/missingReason/));test('evaluation reports scientific accuracy',()=>{const r=w.evaluate([{speciesCorrect:true,healthCorrect:false,scientistAccepted:true}]);assert.equal(r.speciesAccuracy,1);assert.equal(r.healthAccuracy,0);});test('passwords are hashed and verified',()=>{const h=p.hashPassword('long-field-password');assert.ok(!h.includes('long-field-password'));assert.equal(p.verifyPassword('long-field-password',h),true);});
